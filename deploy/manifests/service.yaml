apiVersion: v1
kind: Service
metadata:
  name: data-and-ai-template
  labels:
    app.kubernetes.io/name: data-and-ai-template
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: data-and-ai-template
  ports:
    - name: http
      port: 80
      targetPort: http
