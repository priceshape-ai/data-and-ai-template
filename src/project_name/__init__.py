"""project-name — PriceShape Data & AI project.

`project_name` is a sentinel, not a placeholder token: bootstrap.py renames the
directory and rewrites the imports. When you add a file inside the package, put it
under src/project_name/; when you write *about* the package in prose or docs, use
the {{PACKAGE_NAME}} token instead.
"""

__version__ = "0.1.0"
